/*global define, window, WeakMap, console, Proxy, PEG */
/*jslint indent: 2, todo: true, bitwise: true */

define('blame_parser', ['peg'], function () {
  var parser = Object.create(Object.prototype);

  return parser;
});

// vim: set ts=2 sw=2 sts=2 et :
